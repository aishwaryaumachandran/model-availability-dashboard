# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: model-availability-dashboard
metadata:
  template: model-availability-dashboard
services:
  dashboard:
    project: .
    host: containerapp
    language: python
    docker:
      path: Dockerfile
      remoteBuild: true
